#ifndef GLOBALS_H
#define GLOBALS_H

enum CardType{
    Club = 1,
    Diamond,
    Heart,
    Spade
};

enum CardColor{
    Red = 1,
    Black
};

enum DeckType {
    Start = 1,
    Work,
    Target
};

#endif // GLOBALS_H
